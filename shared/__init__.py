"""Passwordless Azure building blocks shared across the app.

- ``azure_auth.get_credential`` — a tuned ``DefaultAzureCredential``.
- ``fabric.get_fabric_engine``  — a passwordless SQLAlchemy engine for Fabric SQL.
"""
