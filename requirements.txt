# Runtime dependencies
anthropic>=0.40
httpx>=0.27
azure-identity>=1.17
pandas>=2.1
sqlalchemy>=2.0
pyodbc>=5.1
python-dotenv>=1.0
truststore>=0.9

# One-pager Word (.docx) rendering
python-docx>=1.1

# Interactive Teams bot (inbound)
botbuilder-core>=4.15
botbuilder-integration-aiohttp>=4.15
aiohttp>=3.9  # used directly by the bot web app (also pulled in transitively)

# Dev / tests
pytest>=8.0
